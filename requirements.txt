# ==============================================================================
# Dependencies
# ==============================================================================
# This file lists the software ecosystem required to implement the full
# framework. Stages 1–7, 9 are pseudo-code documented in Python style;
# Stage 8 is executable R code.
#
# Researchers should install whichever tools match their implementation choices.

# --- Python (for GIS processing, ML, data wrangling) -------------------------
# Used in Stages 1–7, 9 (pseudo-code — adapt to your toolchain)
geopandas>=0.14
pandas>=2.0
numpy>=1.24
scipy>=1.11
shapely>=2.0
scikit-learn>=1.3
tensorflow>=2.14         # Stage 6: Keras saturation classifier
xgboost>=2.0             # Stage 2: Hierarchical ML (Layer 1)
matplotlib>=3.8
pyyaml>=6.0

# Optional Python libraries
# arcpy                  # If using ArcGIS Pro for spatial operations
# geopy                  # For geocoding in Stage 3
# rasterio               # For satellite imagery processing in Stage 6

# --- R (for econometric estimation) ------------------------------------------
# Stage 8 requires these R packages (install via install.packages()):
#   mlogit   >= 1.1       # Panel mixed logit estimation
#   tidyverse             # Data manipulation (dplyr, tidyr, readr, ggplot2)
#   lubridate             # Date/time handling
#   scales                # Axis formatting for plots
#
# Install in R:
#   install.packages(c("mlogit", "tidyverse", "lubridate", "scales"))

# --- GIS Software (for spatial data preparation) -----------------------------
# Any of the following:
#   ArcGIS Pro >= 3.0     # Used in the GTHA case study
#   QGIS >= 3.30          # Open-source alternative
#   PostGIS               # Database-backed spatial operations
